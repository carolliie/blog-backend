spring.application.name=blog_database
spring.datasource.url=jdbc:postgresql://localhost:5432/blog_database
spring.datasource.username=postgres
spring.datasource.password=carolecarol
spring.datasource.driver-class-name=org.postgresql.Driver


api.security.token.secret=${JWT_SECRET}


spring.mail.host=smtp.gmail.com
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

hibernate.dialect=org.hibernate.dialect.HSQLDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.format-sql=true

minio.url=${MINIO_URL}
minio.access.name=${MINIO_NAME}
minio.access.secret=${MINIO_PASSWORD}
minio.bucket.name=${MINIO_BUCKET}

springdoc.swagger-ui.path=/swagger-ui.html